// styles.ts
import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    h1: {
        color: '#000',
        fontSize: 24,
        fontFamily: 'p1'
    },
    body: {
        color: 'black',
        fontSize: 15,
        fontFamily: 'p2',
    },
    label: {
        fontSize: 12,
        fontFamily: 'label',
        color: 'black',
        opacity: 0.7,
        marginBottom: 10,
        justifyContent: 'center',
    },
});