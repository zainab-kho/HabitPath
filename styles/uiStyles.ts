// styles.ts
import { StyleSheet } from 'react-native';

export const uiStyles = StyleSheet.create({
    inputField: {
        borderWidth: 1,
        borderRadius: 12,
        borderColor: '#000',
        padding: 15,
        marginBottom: 15,
        fontFamily: 'p2',
        fontSize: 14,
    },
});